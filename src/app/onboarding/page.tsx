"use client";

import Onboarding from "@/features/onboarding/components/Onboarding";

const page = () => {
  // TODO: Check if the onboarding is already completed

  return <Onboarding />;
};

export default page;
